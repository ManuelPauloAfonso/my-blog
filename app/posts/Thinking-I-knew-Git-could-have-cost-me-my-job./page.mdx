import PostLayout from "@/components/PostLayout";

export const post = {
  title: "Thinking I knew Git could have cost me my job.",
  description:
    "In this article, I share how I truly learned to use Git in practice  what I can and cannot do when working on a collaborative project, the mistakes I made, and the lessons I learned along the way.",
  author: "Manuel Afonso",
  postedAt: new Date("2025-01-07T00:00:00Z"),
};

export const metadata = {
  title: post.title,
  description: post.description,
};

export const revalidate = 60;

export default (props) => <PostLayout post={post} {...props} />;

;
