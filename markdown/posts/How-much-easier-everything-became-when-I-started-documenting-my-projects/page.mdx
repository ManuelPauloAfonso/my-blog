import PostLayout from "../../components/PostLayout";

export const post = {
  title:
    "How much easier everything became when I started documenting my projects",
  description:
    "A brief overview of the benefits of documenting my projects and how it helped me improve my coding skills.",
  author: "Manuel Afonso",
  postedAt: new Date("2025-01-07T00:00:00Z"),
};

export const metadata = {
  title: post.title,
  description: post.description,
};

export default (props) => <PostLayout post={post} {...props} />;
